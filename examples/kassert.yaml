- assert: deployment/example-app
  namespace: kassert-test
  field: status.readyReplicas
  equals: 2

- assert: configmap/example-config
  namespace: kassert-test
  field: data.APP_ENV
  equals: production

- assert: service/example-service
  namespace: kassert-test
  field: spec.type
  equals: ClusterIP

- assert: pod/example-app-*
  namespace: kassert-test
  field: status.phase
  equals: Running
